namespace Reader {
    std::string testDecimal(std::string text) {
        std::string latStr, lngStr;

        for (int i = 0; i < text.length(); i++) {
            if (text[i]) {
                
            }
        }
    }

    std::string testDMS();
}

